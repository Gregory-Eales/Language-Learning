var back_img;

function setup() {
    createCanvas(600, 600);
    //back_img = loadImage('assets⁩/back.png');
}

function draw() {
    
    while True {
        background(0);
        rect(10, 20, 10, 10);
    }
    
    
}


function draw_ball(x, y) {
    rect(x, y, 10, 10);
    
}